
ignore-paths:
  - docs
  - examples
  - other
  - build
  - dist
  - env

doc-warnings: false

pyroma:
  run: true

vulture:
  run: true

bandit:
  run: true

mccabe:
  run: true

mypy:
  run: true

# run (from dir with .git) via:
# `prospector --profile qdm.prospector.yaml -o grouped:prospector.log ./src/qdm`